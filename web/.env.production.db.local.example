# Copy to .env.production.db.local (gitignored) for one-off prod Prisma commands.
# Get the pooled URL from Neon Console → Connect (same DB as Vercel Production).
DATABASE_URL="postgresql://USER:PASSWORD@HOST/neondb?sslmode=require"
